type Post @model {
  id: ID!
  title: String!
  slug: String!
  content: String!
  actions: [PostHistory]
}
type PostHistory @model {
  id: ID!
  postId: ID!
  creator: String!
  createdAt: String
  payload: HistoryPayload
}

type HistoryPayload {
  title: String
  slug: String
  content: String
}
